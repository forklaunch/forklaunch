node_modules
scripts/catalog/data
*.log

# Captured storefronts. These are build output — a single capture is ~100MB of
# somebody else's images and video, regenerated by crawl.js on demand.
scripts/output/
